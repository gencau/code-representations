hydra:
  job:
    name: ${backbone.name}_${backbone.topk}
  run:
    dir: /Users/gen/workspace/long-context-experiments/bug_localization/output/swe/${hydra:job.name}${run_suffix}
  job_logging:
    root:
      handlers: [console, file]
backbone:
  _target_: src.baselines.backbones.bm25.bm25_retriever.BM25Backbone
  name: bm25-retriever
  data_path: ${data_source.repos_dir}
  topk: 0
  percent: 100 # retrieve the max so we can derive smaller percentages
  k1: 0.9
  b: 0.4
  useDependencyGraph: False

data_source:
  _target_: src.baselines.data_sources.swe_bench_data_source.SWEDataSource
  repos_dir: /Volumes/T9/repos 
  cache_dir: null
  hub_name: /Users/gen/workspace/SWE-Bench/swebench/inference/hf_datasets/SWE-bench_Verified
  split: test

reindex: False
run_suffix: "_percent"